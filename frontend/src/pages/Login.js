import React from 'react';
import UserLogin from '../components/user/UserLogin';

const Login = () => {
    return (
        <div>
            <h1>Login</h1>
            <UserLogin />
        </div>
    );
};

export default Login;